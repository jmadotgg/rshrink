{
  "name": "rshrink",
  "version": "0.0.1",
  "description": "Rshrink for the Web",
  "main": "index.js",
  "repository": "git@github.com:jmadotgg/rshrink.git",
  "author": "Julius Makel <julius-makel@online.de>",
  "license": "MIT",
  "private": true,
   "scripts": {
    "build:wasm-st": "rustup run nightly-2022-04-07 wasm-pack build --target web",
    "build:wasm-mt": "rustup run nightly-2022-04-07 wasm-pack build --target web --out-dir pkg-parallel -- . -Z build-std=panic_abort,std",
    "build:wasm": "run-p build:wasm-st build:wasm-mt",
    "build": "yarn run build:wasm && webpack build ./index.js --mode production -o dist --output-filename index.js && cp index.html dist/"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "comlink": "^4.3.1",
    "wasm-feature-detect": "^1.2.11"
  }
}
